// Import individual modifiers and helpers
import {
  eventListeners as eventListenersModifier,
  popperOffsets as popperOffsetsModifier,
  computeStyles as computeStylesModifier,
  applyStyles as applyStylesModifier,
} from "./modifiers";
import { detectOverflow as detectOverflowHelper } from "./helpers";

// Define the default modifiers as an array of objects
const defaultModifiers = [
  { name: "eventListeners", options: {} },
  { name: "popperOffsets", options: {} },
  { name: "computeStyles", options: {} },
  { name: "applyStyles", options: {} },
];

// Import popperGenerator from createPopper.js
const { popperGenerator } = require("./createPopper.js");

// Create the createPopper function with the default modifiers
const createPopper = popperGenerator({
  defaultModifiers,
});

// Export only the necessary functions
export { createPopper, detectOverflowHelper };
