export default function getVariation(placement) {
  if (typeof placement !== 'string') {
    throw new Error('Expected a string as argument');
  }

  const parts = placement.split('-');

  if (parts.length < 2) {
    throw new Error('Placement string must contain at least one "-"');
  }

  return parts[1];
}
